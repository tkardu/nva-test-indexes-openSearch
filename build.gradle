plugins{
    id 'nva.search.service.rootplugin'
}

//workaround for jacoco-merge to work
allprojects{
    apply plugin: 'nva.search.service.java-common-conventions'
}

wrapper {
    gradleVersion = '7.2'
    distributionType = Wrapper.DistributionType.ALL
}
