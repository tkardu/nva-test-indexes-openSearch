{
  batch:{
    index:{
      eventbusname = default
      eventbusname = ${?BATCH_INDEX_EVENT_BUS_NAME}
      recursion = true
      recursion = ${?BATCH_INDEX_ENABLE_RECURSION}
      number_of_files_per_event= 1000
      number_of_files_per_event=${?NUMBER_OF_FILES_PER_EVENT}
     }
  }
}